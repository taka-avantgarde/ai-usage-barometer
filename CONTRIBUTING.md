# Contributing

Run `bash -n ai-usage.60s.sh install.sh` and `./tests/run.sh` before opening a pull request. Never include authentication tokens, Keychain contents, `~/.codex/auth.json`, or private session logs in issues or test fixtures.
